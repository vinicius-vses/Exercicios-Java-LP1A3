/*package Q2.src.Q2;

public class CalculaMediaPonderada{
    double media;

    public void setMedia(double media) {
        this.media = media;
    }

    public double getMedia(MediaAlunoDisciplina mad) {
        double media = (mad.getMediaProva() * mad.getPesoProva()
                + mad.getMediaTrab() * mad.getMediaProva()) / 10;
        if (media < 0 || media > 10)
            System.out.println("A nota esta fora da escala!");
        return media;
    }
}*/
